"""
Reusable UI Components — Well Dashboard Frontend

Components are stateless (or lightly stateful) widgets that can be dropped
into any page. Import the function you need directly:

    from frontend.components.status_badge import status_badge
    from frontend.components.data_table import render_data_table
    from frontend.components.survey_chart import render_survey_charts

Available components
--------------------
auth        Login / register forms, logout button
navigation  Top tab bar and sidebar
status_badge Coloured status label (HTML span)
data_table  Standardised dataframe renderer
survey_chart Plan vs Actual survey visualisation (Plotly)
well_form   Create-well form (shared between Projects and Well Data pages)
"""
